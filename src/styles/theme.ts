import * as Color from 'color';

const themeColor = '#FA7268';

const blackColor = '#333333';
const garyColor = '#8C8C8C';

const theme = {
  themeColor,

  blackColor,
  garyColor,

  defaultBorder: '1px solid #e9e9e9',
};

export default theme;
